import React from 'react';
import ContentLoader from 'react-content-loader';

const SidebarLoader = () => {
  return (
    <ContentLoader viewBox={'0 0 425 700'}>
      <rect x={'0'} y={'0'} rx={'0'} rx={'0'} width={'425'} height={`700`} />
    </ContentLoader>
  );
};

export default SidebarLoader;
