import React from 'react';
import ContentLoader from 'react-content-loader';

const LineLoader = () => {
  return (
    <ContentLoader viewBox={'0 0 648 30'}>
      <rect x={'0'} y={`-10`} width={'648'} height={`30`} />
    </ContentLoader>
  );
};

export default LineLoader;
