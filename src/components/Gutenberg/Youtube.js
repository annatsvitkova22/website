import React from 'react';
import PropTypes from 'prop-types';

const Youtube = ({ block }) => {
  return <div>Youtube block</div>;
};

export default Youtube;

Youtube.propTypes = {
  block: PropTypes.any,
};
